const game = ()=> {
    let pScore = 0;
    let cScore = 0;
    //Play Game
    const playGame = ()=> {
        const options = document.querySelectorAll(".options-outer buttons");
        const playerHand = document.querySelector(".player-hand");
        const computerHand = document.querySelector(".computer-hand");
    }
    playGame();
    console.log(pScore);
    console.log(cScore);
}
game();
